<Window x:Class="Romulus.UI.Wpf.MessageDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Meldung"
    Width="440" Height="200"
    MinWidth="300" MinHeight="150"
    WindowStartupLocation="CenterOwner"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    WindowStyle="SingleBorderWindow"
    FontFamily="Segoe UI" FontSize="13"
    Background="{DynamicResource BrushBackground}"
    Foreground="{DynamicResource BrushTextPrimary}"
    UseLayoutRounding="True">

  <Window.InputBindings>
    <KeyBinding Key="Escape" Command="Close"/>
  </Window.InputBindings>

  <Window.CommandBindings>
    <CommandBinding Command="Close" Executed="OnCloseCommand"/>
  </Window.CommandBindings>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
      <!-- Icon -->
      <TextBlock x:Name="txtIcon" FontFamily="Segoe MDL2 Assets" FontSize="32"
                 VerticalAlignment="Top" Margin="0,0,16,0"
                 Foreground="{DynamicResource BrushAccentCyan}"/>
      <!-- Message -->
      <TextBlock x:Name="txtMessage" TextWrapping="Wrap" VerticalAlignment="Center"
                 MaxWidth="340" Foreground="{DynamicResource BrushTextPrimary}"/>
    </StackPanel>

    <!-- Buttons -->
    <StackPanel x:Name="panelButtons" Grid.Row="1" Orientation="Horizontal"
                HorizontalAlignment="Right" Margin="0,16,0,0"/>
  </Grid>
</Window>
