<Window x:Class="Romulus.UI.Wpf.DangerConfirmDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Bestätigung erforderlich"
    Width="480" SizeToContent="Height"
    MinWidth="360" MinHeight="280"
    WindowStartupLocation="CenterOwner"
    ShowInTaskbar="False"
    ResizeMode="NoResize"
    WindowStyle="SingleBorderWindow"
    FontFamily="Segoe UI" FontSize="13"
    Background="{DynamicResource BrushBackground}"
    Foreground="{DynamicResource BrushTextPrimary}"
    UseLayoutRounding="True">

  <Window.InputBindings>
    <KeyBinding Key="Escape" Command="Close"/>
  </Window.InputBindings>
  <Window.CommandBindings>
    <CommandBinding Command="Close" Executed="OnCloseCommand"/>
  </Window.CommandBindings>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Danger icon + title -->
    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
      <TextBlock Text="&#xEA39;" FontFamily="Segoe MDL2 Assets" FontSize="28"
                 Foreground="{DynamicResource BrushDanger}" VerticalAlignment="Center" Margin="0,0,12,0"/>
      <TextBlock x:Name="txtTitle" FontSize="16" FontWeight="Bold"
                 Foreground="{DynamicResource BrushDanger}" VerticalAlignment="Center"/>
    </StackPanel>

    <!-- Message -->
    <TextBlock x:Name="txtMessage" Grid.Row="1" TextWrapping="Wrap"
               Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,16"/>

    <!-- Confirmation input -->
    <StackPanel Grid.Row="2" Margin="0,0,0,8">
      <TextBlock x:Name="txtConfirmHint" Foreground="{DynamicResource BrushTextMuted}" FontSize="12"
                 Margin="0,0,0,4"/>
      <TextBox x:Name="txtConfirmInput" Padding="8,6" FontFamily="Consolas"
               Background="{DynamicResource BrushSurface}" Foreground="{DynamicResource BrushTextPrimary}"
               BorderBrush="{DynamicResource BrushDanger}" CaretBrush="{DynamicResource BrushDanger}"
               TextChanged="OnConfirmTextChanged"
               AutomationProperties.Name="{Binding ElementName=txtConfirmHint, Path=Text}"/>
    </StackPanel>

    <!-- Buttons -->
    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
      <Button Content="Abbrechen" Width="100" Margin="0,0,8,0" IsCancel="True" Click="OnCancel"
              AutomationProperties.Name="Abbrechen"/>
      <Button x:Name="btnConfirm" Width="120" IsEnabled="False" Click="OnConfirm"
              Background="{DynamicResource BrushDanger}" Foreground="White" FontWeight="Bold"
              AutomationProperties.Name="Endgültig bestätigen">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                     VerticalAlignment="Center" Margin="0,0,6,0"/>
          <TextBlock x:Name="txtBtnLabel" Text="Bestätigen" VerticalAlignment="Center"/>
        </StackPanel>
      </Button>
    </StackPanel>
  </Grid>
</Window>
