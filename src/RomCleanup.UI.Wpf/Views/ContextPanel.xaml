<UserControl x:Class="RomCleanup.UI.Wpf.Views.ContextPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
>
  <Border Background="{DynamicResource BrushSurfaceAlt}"
          BorderBrush="{DynamicResource BrushBorder}"
          BorderThickness="1,0,0,0"
          Padding="12,12,12,10">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel>
        <TextBlock Text="{Binding Loc[Context.Title]}" Style="{StaticResource SectionHeader}" Margin="0,0,0,10"/>

        <Border Style="{StaticResource SectionCard}" Margin="0,0,0,8">
          <StackPanel>
            <TextBlock Text="{Binding Loc[Context.RunStatus]}" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,6"/>
            <TextBlock Text="{Binding StatusReady}" Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding StatusRuntime}" Foreground="{DynamicResource BrushTextMuted}" FontSize="11"/>
          </StackPanel>
        </Border>

        <Border Style="{StaticResource SectionCard}" Margin="0,0,0,8"
                Visibility="{Binding SelectedNavTag, Converter={StaticResource StrEqualsToVis}, ConverterParameter=Start}">
          <StackPanel>
            <TextBlock Text="{Binding Loc[Context.StartHintsTitle]}" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,6"/>
            <TextBlock Text="{Binding Loc[Context.StartHint1]}" Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding Loc[Context.StartHint2]}" Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,2"/>
            <TextBlock Text="{Binding Loc[Context.StartHint3]}" Foreground="{DynamicResource BrushTextPrimary}"/>
          </StackPanel>
        </Border>

        <Border Style="{StaticResource SectionCard}" Margin="0,0,0,8"
                Visibility="{Binding SelectedNavTag, Converter={StaticResource StrEqualsToVis}, ConverterParameter=Setup}">
          <StackPanel>
            <TextBlock Text="{Binding Loc[Context.SetupQuickcheck]}" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,6"/>
            <TextBlock Text="{Binding MoveApplyGateText}" TextWrapping="Wrap" Foreground="{DynamicResource BrushTextPrimary}" FontSize="11" Margin="0,0,0,6"/>
            <Button Content="{Binding Loc[Context.RunPreview]}" Command="{Binding QuickPreviewCommand}"/>
          </StackPanel>
        </Border>

        <Border Style="{StaticResource SectionCard}" Margin="0,0,0,8"
                Visibility="{Binding SelectedNavTag, Converter={StaticResource StrEqualsToVis}, ConverterParameter=Analyse}">
          <StackPanel>
            <TextBlock Text="{Binding Loc[Context.AnalysisMetrics]}" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,6"/>
            <TextBlock Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,2">
              <Run Text="{Binding Loc[Context.ModeLabel]}"/><Run Text="{Binding DashMode}"/>
            </TextBlock>
            <TextBlock Foreground="{DynamicResource BrushTextPrimary}" Margin="0,0,0,2">
              <Run Text="{Binding Loc[Context.DupesLabel]}"/><Run Text="{Binding DashDupes}"/>
            </TextBlock>
            <TextBlock Foreground="{DynamicResource BrushTextPrimary}">
              <Run Text="{Binding Loc[Context.JunkLabel]}"/><Run Text="{Binding DashJunk}"/>
            </TextBlock>
          </StackPanel>
        </Border>

        <Border Margin="0,0,0,8">
          <Border.Style>
            <Style TargetType="Border" BasedOn="{StaticResource SectionCard}">
              <Setter Property="Visibility" Value="Collapsed"/>
              <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedNavTag}" Value="Analyse">
                  <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding SelectedNavTag}" Value="Setup">
                  <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </Border.Style>
          <StackPanel>
            <TextBlock Text="{Binding Loc[Context.SafetyGate]}" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,6"/>
            <TextBlock Text="{Binding MoveApplyGateText}" TextWrapping="Wrap" Foreground="{DynamicResource BrushTextPrimary}" FontSize="11"/>
          </StackPanel>
        </Border>

        <Border Style="{StaticResource SectionCard}" Visibility="{Binding SelectedNavTag, Converter={StaticResource StrEqualsToVis}, ConverterParameter=System}">
          <StackPanel>
            <TextBlock Text="{Binding Loc[Context.SystemActions]}" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextMuted}" Margin="0,0,0,6"/>
            <Button Content="{Binding Loc[Start.FlowOpenSetup]}" Command="{Binding GoToSetupCommand}" Margin="0,0,0,6"/>
            <Button Content="{Binding Loc[Start.FlowOpenAnalyse]}" Command="{Binding GoToAnalyseCommand}" Margin="0,0,0,6"/>
            <Button Content="{Binding Loc[Report.OpenReportLog]}" Command="{Binding OpenReportCommand}"/>
          </StackPanel>
        </Border>
      </StackPanel>
    </ScrollViewer>
  </Border>
</UserControl>
