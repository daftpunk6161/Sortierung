name: test-pipeline

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/**'
      - 'data/**'
      - '.github/workflows/test-pipeline.yml'
  pull_request:
    paths:
      - 'src/**'
      - 'data/**'
      - '.github/workflows/test-pipeline.yml'

jobs:
  tests:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'ga'

      - name: NuGet Cache
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('src/**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore src/RomCleanup.sln

      - name: Build
        run: dotnet build src/RomCleanup.sln --no-restore --configuration Release

      - name: Journey Matrix Gate
        run: |
          dotnet test src/RomCleanup.Tests/RomCleanup.Tests.csproj --no-build --configuration Release --filter "FullyQualifiedName~ApiIntegrationTests|FullyQualifiedName~CliProgramTests|FullyQualifiedName~GuiViewModelTests|FullyQualifiedName~WpfNewTests|FullyQualifiedName~FeatureCommandServiceTests|FullyQualifiedName~RunServiceAndSettingsTests|FullyQualifiedName~RunManagerTests|FullyQualifiedName~RunManagerAdvancedTests|FullyQualifiedName~AuditCsvStoreTests|FullyQualifiedName~AuditSigningServiceTests|FullyQualifiedName~SettingsLoaderTests"

      - name: Unit + Coverage Gate
        run: |
          dotnet test src/RomCleanup.sln --no-build --configuration Release --collect:"XPlat Code Coverage" --logger "trx;LogFileName=test-results.trx" -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=cobertura
          # Install and run coverage threshold check
          dotnet tool install --global dotnet-reportgenerator-globaltool 2>$null
          $coverageFiles = Get-ChildItem -Recurse -Filter "coverage.cobertura.xml" | Select-Object -First 1
          if ($coverageFiles) {
            [xml]$cov = Get-Content $coverageFiles.FullName
            $lineRate = [double]$cov.coverage.'line-rate' * 100
            Write-Host "Line coverage: $($lineRate.ToString('F1'))%"
            if ($lineRate -lt 50) {
              throw "Coverage $($lineRate.ToString('F1'))% is below minimum threshold of 50%"
            }
          }

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            src/RomCleanup.Tests/TestResults/**/*.trx
            src/RomCleanup.Tests/TestResults/**/coverage.cobertura.xml

  governance:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'ga'

      - name: Build (warnings as errors)
        run: dotnet build src/RomCleanup.sln --configuration Release /p:TreatWarningsAsErrors=true

      - name: Verify dependency direction
        shell: pwsh
        run: |
          # Core darf nicht Infrastructure oder Entry Points referenzieren
          $coreProj = Get-Content src/RomCleanup.Core/RomCleanup.Core.csproj -Raw
          if ($coreProj -match 'RomCleanup\.Infrastructure|RomCleanup\.CLI|RomCleanup\.Api|RomCleanup\.UI\.Wpf') {
            throw 'Core project must not reference Infrastructure or Entry Points'
          }
          # Contracts darf nichts anderes referenzieren
          $contractsProj = Get-Content src/RomCleanup.Contracts/RomCleanup.Contracts.csproj -Raw
          if ($contractsProj -match 'RomCleanup\.Core|RomCleanup\.Infrastructure|RomCleanup\.CLI|RomCleanup\.Api|RomCleanup\.UI\.Wpf') {
            throw 'Contracts project must not reference any other project'
          }
          Write-Host 'Dependency direction OK'

      - name: Verify using-statement boundaries
        shell: pwsh
        run: |
          # Core .cs files must not use Infrastructure/CLI/Api/UI namespaces
          $violations = Get-ChildItem -Recurse src/RomCleanup.Core -Filter *.cs |
            Select-String -Pattern 'using\s+RomCleanup\.(Infrastructure|CLI|Api|UI)' |
            ForEach-Object { "$($_.Path):$($_.LineNumber): $($_.Line.Trim())" }
          if ($violations) { $violations; throw "Core has disallowed using-statements" }
          # Contracts .cs files must not use any other project namespace
          $violations = Get-ChildItem -Recurse src/RomCleanup.Contracts -Filter *.cs |
            Select-String -Pattern 'using\s+RomCleanup\.(Core|Infrastructure|CLI|Api|UI)' |
            ForEach-Object { "$($_.Path):$($_.LineNumber): $($_.Line.Trim())" }
          if ($violations) { $violations; throw "Contracts has disallowed using-statements" }
          Write-Host 'Using-statement boundaries OK'

  windows-ui:
    runs-on: windows-latest
    needs: tests
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'ga'

      - name: Restore & Build
        run: dotnet build src/RomCleanup.sln --configuration Release

      - name: WPF Accessibility & Automation Tests
        shell: pwsh
        run: |
          dotnet test src/RomCleanup.Tests/RomCleanup.Tests.csproj --no-build --configuration Release --filter "FullyQualifiedName~Wpf|FullyQualifiedName~Theme|FullyQualifiedName~Accessibility" --logger "trx;LogFileName=wpf-test-results.trx"

      - name: Upload WPF test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wpf-test-results
          path: src/RomCleanup.Tests/TestResults/**/*.trx

  sbom:
    runs-on: windows-latest
    needs: tests
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'ga'

      - name: Generate SBOM
        shell: pwsh
        run: |
          dotnet tool install --global Microsoft.Sbom.DotNetTool 2>$null
          dotnet build src/RomCleanup.sln --configuration Release
          sbom-tool generate -b src -bc src -pn RomCleanup -pv "${{ github.ref_name }}" -ps RomCleanupTeam -nsb https://github.com/${{ github.repository }}

      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: src/_manifest/**

  mutation:
    runs-on: windows-latest
    needs: tests
    continue-on-error: true
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
          dotnet-quality: 'ga'

      - name: Run Stryker Mutation Tests
        shell: pwsh
        run: |
          dotnet tool install --global dotnet-stryker 2>$null
          dotnet stryker --project src/RomCleanup.Core/RomCleanup.Core.csproj --test-project src/RomCleanup.Tests/RomCleanup.Tests.csproj --reporter "html" --reporter "json" --threshold-high 80 --threshold-low 60 --threshold-break 40

      - name: Upload Mutation Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mutation-report
          path: StrykerOutput/**/reports/**
